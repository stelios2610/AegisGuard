# AegisGuard VPN fail2ban filter
# Place in /etc/fail2ban/filter.d/aegisguard-vpn.conf

[Definition]
failregex = ^.* <HOST>:\d+ TLS Auth Error: Auth Username/Password verification failed.*$
            ^.* <HOST>:\d+ SENT CONTROL \[UNDEF\]: 'AUTH_FAILED'.*$
ignoreregex =
